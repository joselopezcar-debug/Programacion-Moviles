import Foundation

// definir el promedio por pesos
let parcial = 0.3
let trabajo = 0.3
let final = 0.4

print("Nombre del Alumno ")
let alumno = readLine() ?? ""

print("Ingrese la nota parcial ")
let parcialNota = Double(readLine() ?? "0") ?? 0
 // Commit de ingreso de datos
let trabajoNota = Double(readLine() ?? "0") ?? 0
let finalNota = Double(readLine() ?? "0") ?? 0
 // commit del calculo
 // commit de mostrar los datos
